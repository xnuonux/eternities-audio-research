# measurements

Harness output lands here, one directory per plugin. JSON reports are committed; raw `.npy`
impulse responses and rendered audio are gitignored (they are large and regenerable).

A report without its `parameters` block is not reproducible. The harness records it automatically.
